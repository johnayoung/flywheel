{
  "id": "in-loop-verify-data-taxonomy",
  "goal": "Drive a fixture through the real orchestrate loop with a scripted invoker so telemetry lands in the run JSONL, aggregates land on attempt rows, a steering command round-trips into a ledger event end-to-end, and a v(N-1) store is refused with StoreSchemaError per the no-migration policy.",
  "prerequisites": [
    "telemetry-sink",
    "attempt-aggregates",
    "telemetry-write-path",
    "readers-tail-file",
    "remove-run-log-rerender",
    "schema-reduction",
    "steering-ledger",
    "worldstate-pinning"
  ],
  "tags": ["in-loop-verification"],
  "context": {
    "relevant": [
      "packages/flywheel-worktree/tests/test_worker.py",
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_orchestrate.py",
      "packages/flywheel-core/src/flywheel_core/harness.py",
      "packages/flywheel-core/src/flywheel_core/store_sqlite.py",
      "packages/flywheel-core/src/flywheel_core/invoker_client.py"
    ],
    "references": [
      ".flywheel/specs/00017-FEATURE-in-loop-verification-gate.md FR-3 (both loop-produced ends) and FR-4 (schema-version coverage)",
      "packages/flywheel-worktree/tests/test_worker.py -- model for driving a full real-loop cycle with a scripted invoke against a real store"
    ],
    "constraints": [
      "FR-3: drive BOTH loop-produced ends -- the harness produces the new artifacts (sink lines, aggregates, steering event) AND the real loop consumes/applies them. Stubbing either end fails the slot.",
      "FR-4 adapted per spec 00025's migration decision: seed a store fixture at the previous schema version and assert the real SqliteStore refuses it with StoreSchemaError -- a fresh current-schema store does NOT satisfy this",
      "Use the existing injectable invoker seam fed a deterministic scripted envelope; never call a paid/network agent",
      "Never read, write, or migrate .flywheel/flywheel.sqlite -- fixture/temp stores only",
      "Tag MUST include in-loop-verification verbatim -- archive_completed_phases keys off this exact tag",
      "Commit with a Conventional Commits message (test: ...) before emitting intent=verify"
    ],
    "non_goals": [
      "Do not stub orchestrate, the harness, or SqliteStore -- only the agent's text is scripted",
      "Do not weaken any purity test to make the fixture fit"
    ]
  },
  "graders": [
    { "type": "command", "run": "uv run pytest packages/flywheel-worktree/tests/test_in_loop_data_taxonomy.py", "name": "in-loop-verify" },
    { "type": "command", "run": "uv run pytest", "name": "full-suite" }
  ]
}
