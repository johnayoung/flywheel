{
  "graders": [
    {
      "type": "command",
      "run": "grep -qE 'ANTHROPIC_API_KEY|claude login' README.md",
      "name": "visible-readme-auth-reference",
      "rung": "command-end-state"
    },
    {
      "type": "command",
      "run": "uv run pytest .flywheel/holdout/00028-onboarding-preflight-and-auth -k next_steps_auth -q",
      "name": "held-out-next-steps-auth",
      "rung": "command-end-state-out-of-band"
    },
    {
      "type": "command",
      "run": "uv run pytest packages/flywheel-orchestrator/tests/test_init.py -q",
      "name": "seam-holdout-init-suite",
      "rung": "composition-holdout"
    }
  ],
  "id": "onboard-auth-docs",
  "goal": "Both the README quickstart and the flywheel init next-steps output name the agent-authentication step (ANTHROPIC_API_KEY or `claude login`) required before the first worker run.",
  "tags": ["onboarding", "docs", "auth", "spec-00028"],
  "prerequisites": ["onboard-init-git-preflight"],
  "context": {
    "relevant": [
      "README.md -- the Quickstart block (lines 5-15: uv sync -> flywheel init -> flywheel worker --once); add an agent-auth step naming ANTHROPIC_API_KEY or `claude login` before the first worker run",
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_workflow.py -- _print_init_next_steps (2155): its 'Next steps:' stdout (printed after init writes the policy and on the existing-file path) must reference authenticating the agent (ANTHROPIC_API_KEY or `claude login`)",
      "docs/research/2026-06-17-adoption-readiness-audit.md -- finding B4 (zero references to ANTHROPIC_API_KEY/claude login anywhere in the onboarding surface)"
    ],
    "constraints": [
      "BOTH surfaces must carry the fact: the README quickstart AND the init next-steps stdout. Documenting it in only one place leaves a path to the opaque SDK failure (D-3, criterion 7 defense).",
      "Reference ANTHROPIC_API_KEY or `claude login` (the regex the graders match is `ANTHROPIC_API_KEY|claude login`).",
      "Do NOT add a live auth probe or import the SDK in init -- init must not hard-import the optional claude-agent-sdk extra (D-3 / CLAUDE.md lazy-SDK-boundary invariant). This is a text/print surface only.",
      "Commit the change with a clear message before reporting done."
    ],
    "non_goals": [
      "Do not modify files under .flywheel/holdout/ -- the out-of-band held-out grading surface for the next-steps half of criterion 7 (authored separately); do not create, read, or weaken anything there.",
      "Do not weaken or delete existing init-test assertions (e.g. the 'Next steps:' / 'created: flywheel.toml' assertions in test_init.py); criterion 10 requires a named equal-or-stronger replacement for any relaxed check.",
      "Do not validate the credential or call the SDK (out of scope: this documents the step and the failure, it does not probe auth).",
      "Do not change the preflight, sandbox, or worker surfaces -- those are sibling tasks."
    ],
    "edge_cases": [
      "The auth line must appear on the post-init terminal output path the adopter actually sees (the next-steps print), not only inside a docs file the quickstart never points to -- a README-only or docs-page-only fix fails the next-steps held-out check (criterion 7 defense)."
    ],
    "notes": [
      "README grep is the visible shell grader; the next-steps stdout assertion is held-out under .flywheel/holdout/00028-onboarding-preflight-and-auth and runs out-of-band (criterion 11)."
    ]
  }
}
