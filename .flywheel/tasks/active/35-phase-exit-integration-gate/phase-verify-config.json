{
  "id": "phase-verify-config",
  "goal": "flywheel.toml exposes a [phase] verify key parsed into WorkPolicy.phase_verify (default None; empty/non-string raises PolicyError), mirroring the existing optional-string policy keys, and init scaffolds a commented [phase] block distinct from the other tables.",
  "graders": [
    {
      "type": "command",
      "name": "phase-verify-policy",
      "run": "uv run pytest packages/flywheel-orchestrator/tests/test_policy.py -k phase_verify -q"
    },
    {
      "type": "command",
      "name": "init-and-policy-suite",
      "run": "uv run pytest packages/flywheel-orchestrator/tests/test_policy.py packages/flywheel-orchestrator/tests/test_init.py -q"
    },
    {
      "type": "command",
      "name": "full-suite",
      "run": "uv run pytest -q"
    }
  ],
  "prerequisites": [],
  "tags": ["policy", "config", "phase", "spec-00035"],
  "context": {
    "relevant": [
      ".flywheel/specs/00035-FEATURE-phase-exit-integration-gate.md -- authoritative spec; config half of criteria 1-4, D-1/D-3",
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_policy.py -- WorkPolicy dataclass + load_policy; add phase_verify: str | None = None, parsed from [phase] verify, mirroring an existing optional-string key (e.g. submit_base / sandbox_setup): empty-string or non-string -> PolicyError; absent -> None",
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_workflow.py -- _INIT_POLICY_TAIL (the init scaffolding renderer): append a commented [phase] verify block as a DISTINCT table, not overlapping [submit]/[sandbox]",
      "packages/flywheel-orchestrator/tests/test_policy.py -- add visible [phase] verify parse cases (present/absent/empty/non-string), no existing assertion weakened"
    ],
    "references": [
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_policy.py -- submit_base (spec 00026) and sandbox_setup (spec 00028) are the exact pattern to mirror for an optional-string [table] key with default None and PolicyError on empty/non-string"
    ],
    "constraints": [
      "Key is [phase] verify -> WorkPolicy.phase_verify (default None). Empty string or non-string value raises PolicyError; absent [phase] table and absent key both yield None without error (back-compat, D-3).",
      "The init [phase] block is commented-out by default so init --defaults produces phase_verify=None (the back-compat default); it is a distinct toml table appended after the existing [submit]/[sandbox] blocks, neither rewriting the other.",
      "A pre-existing flywheel.toml with no [phase] table still loads without error.",
      "Commit with a clear conventional-commit message; no emojis.",
      "Run mcp__ide__getDiagnostics on every .py you touch and fix all Pylance errors before reporting done."
    ],
    "non_goals": [
      "Do not implement the archive-time gate behavior here -- that is the dependent task (phase-verify-gate).",
      "Do not ship a default phase-verify command value (operator-configured; unset = today's behavior).",
      "Do not weaken existing policy/init tests or rewrite the [submit]/[sandbox] init blocks."
    ],
    "edge_cases": [
      "[phase] table present but verify key absent -> phase_verify None.",
      "verify = \"\" (empty) or a non-string -> PolicyError.",
      "No [phase] table at all -> None, no error."
    ],
    "notes": [
      "Config layer; the gate layer depends on this (spec Decomposition Hint). Graders here are visible policy tests (the parse contract); the gate behavior is held-out-graded in the dependent task."
    ]
  }
}
