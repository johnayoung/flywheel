{
  "id": "phase-verify-gate",
  "goal": "At phase archival, when WorkPolicy.phase_verify is configured the worker runs that command against the merged phase base in repo_root and archives the phase only if it exits 0 -- a non-zero exit leaves the phase active with an observable reason -- while phase_verify=None preserves today's archival exactly.",
  "prerequisites": ["phase-verify-config"],
  "graders": [
    {
      "type": "command",
      "name": "phase-gate-held-out",
      "run": "uv run pytest .flywheel/holdout/00035-phase-exit-integration-gate/ -q"
    },
    {
      "type": "command",
      "name": "archival-suite-intact",
      "run": "uv run pytest packages/flywheel-worktree/tests packages/flywheel-orchestrator/tests -k \"archive or phase\" -q"
    },
    {
      "type": "command",
      "name": "no-tests-dropped",
      "run": "test \"$(uv run pytest packages/flywheel-worktree packages/flywheel-orchestrator --collect-only -q 2>/dev/null | grep -c '::')\" -ge 430"
    },
    {
      "type": "command",
      "name": "full-suite",
      "run": "uv run pytest -q"
    }
  ],
  "tags": ["worktree", "orchestrator", "phase", "gate", "spec-00035"],
  "context": {
    "relevant": [
      ".flywheel/specs/00035-FEATURE-phase-exit-integration-gate.md -- authoritative spec; criteria 1-5, D-1/D-2/D-3",
      "packages/flywheel-worktree/src/flywheel_worktree/worker.py -- archive_phases (and the orchestrator archive_completed_phases call site): before archiving an eligible phase, if policy.phase_verify is set, run it (shell) in repo_root against the merged base and gate archival on exit code",
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_history.py / _workflow.py -- archive_completed_phases is where a phase's archival transition happens; the gate wraps that transition",
      "packages/flywheel-core/src/flywheel_orchestrator -- WorkPolicy.phase_verify (added by the prerequisite task) is the command to run"
    ],
    "references": [
      "packages/flywheel-worktree/tests/test_worker.py -- test_record_phase_bases_captures_once_and_idempotent and the archival tests show how phases archive today; the gate must not change the None path",
      "the gate runs `subprocess.run(phase_verify, shell=True, cwd=repo_root)` against the merged base (D-2); exit 0 archives, non-zero leaves the phase active and records the gate failure in the phase's outcome/state"
    ],
    "constraints": [
      "The phase-verify command runs in repo_root against the MERGED base (the landed integration result), never inside a task sandbox (D-2, criterion 3).",
      "Exit 0 archives the phase exactly as the no-gate path does (criterion 2); non-zero leaves the phase active/unarchived and surfaces the failure through the worker's recorded outcome, not only ephemeral stderr (criterion 1).",
      "phase_verify=None takes the exact archival path that exists today -- no gate, no behavior change (criterion 4, D-3); existing phase-base recording, idempotency, and retention sweep are unchanged.",
      "Do not retry or auto-fix a failed gate; do not add a new notification surface beyond the existing phase log/state.",
      "Commit with a clear conventional-commit message; no emojis.",
      "Run mcp__ide__getDiagnostics on every .py you touch and fix all Pylance errors before reporting done."
    ],
    "non_goals": [
      "Do not author, edit, weaken, delete, or read anything under .flywheel/holdout/00035-phase-exit-integration-gate/.",
      "Do not change WorkPolicy.phase_verify parsing (owned by the prerequisite task).",
      "Do not change per-task grading, submit-time re-verification, or landing/base-resolution mechanics.",
      "Do not interpret a structural/LLM review verdict in the harness -- the harness only runs a command and reads its exit code (D-1).",
      "Do not weaken existing archival/idempotency/retention tests."
    ],
    "edge_cases": [
      "Configured gate exits non-zero -> phase stays in its active location, failure recorded (criterion 1).",
      "Configured gate exits 0 -> phase archives identically to the no-gate path (criterion 2).",
      "Gate command observes a base-only fact present on the merged base but absent in any task sandbox -> proves it ran against repo_root's merged base, not a sandbox (criterion 3).",
      "phase_verify=None -> archival end-state byte-identical to today (criterion 4)."
    ],
    "notes": [
      "Held-out oracle under .flywheel/holdout/00035-phase-exit-integration-gate/ drives the real archive step with a configured phase_verify (true/false/marker-writing command) and asserts archived-vs-active phase state + the cwd/base the gate observed. Outside pytest testpaths; collected explicitly.",
      "Gate layer; depends on the config layer (spec Decomposition Hint)."
    ]
  }
}
