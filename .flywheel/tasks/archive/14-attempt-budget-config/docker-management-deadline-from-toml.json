{
  "id": "docker-management-deadline-from-toml",
  "goal": "Docker management calls issued by the container backend run under the [deadlines] docker_management_seconds ceiling resolved from flywheel.toml rather than the module-import-time default.",
  "tags": ["config", "budgets", "container", "spec-00066"],
  "prerequisites": ["attempt-budgets-from-toml"],
  "conflict_keys": [
    "packages/flywheel-worktree/src/flywheel_worktree/worker.py"
  ],
  "graders": [
    { "type": "command", "name": "policy-override-behavior", "run": "uv run pytest packages/flywheel-container/tests/test_docker_management_policy_override.py" },
    { "type": "command", "name": "existing-deadline-and-wrap-surface", "run": "uv run pytest packages/flywheel-container/tests/test_docker_management_deadline.py packages/flywheel-container/tests/test_container_strategy.py packages/flywheel-worktree/tests/test_worker_backend_select.py" }
  ],
  "context": {
    "relevant": [
      "packages/flywheel-container/src/flywheel_container/_docker.py -- management primitives take timeout: float | None = DEFAULT_MANAGEMENT_TIMEOUT (line 42, resolved at import); leave the primitives' signatures and defaults intact",
      "packages/flywheel-container/src/flywheel_container/_submit.py -- the strategy's management call sites (check_image_uid, ensure_internal_network, remove_container, the atexit force-remove) must pass the configured timeout",
      "packages/flywheel-worktree/src/flywheel_worktree/worker.py -- maybe_wrap_for_backend (line 2351) builds the container strategy from policy; pass policy.deadlines.docker_management_seconds into build_container_strategy here. LARGE FILE: use serena find_symbol",
      "packages/flywheel-container/tests/test_docker_management_deadline.py -- the committed stub-docker deadline suite; keep green and mirror its stub pattern"
    ],
    "constraints": [
      "flywheel-container must not import flywheel_orchestrator (it stays a sibling consumer): build_container_strategy gains a plain management_timeout: float | None keyword defaulting to DEFAULT_MANAGEMENT_TIMEOUT, so library callers are byte-identical; the worktree wiring supplies the policy-resolved value (None policy -> DeadlineConfig() default, which equals the module constant).",
      "packages/flywheel-container/tests/test_docker_management_policy_override.py (create at exactly this path) must prove the override reaches the management calls: a strategy built with a tiny management_timeout against a stub docker that hangs raises the docker timeout error in bounded wall time, and the 0/None opt-out is asserted at the call seam (captured timeout argument), never by waiting.",
      "Commit the change with a clear message before reporting done."
    ],
    "non_goals": [
      "Do not regress the already-bounded paths: start_container keeps its 120s bound and exec keeps exec_timeout (spec 00066 out-of-scope line).",
      "Do not modify existing tests; do not weaken or delete assertions.",
      "Do not change DEFAULT_MANAGEMENT_TIMEOUT or the resolver defaults; no docs edits (sibling task)."
    ],
    "edge_cases": [
      "The atexit force_remove_container_sync path must honor the configured ceiling too -- spec 00066 criterion 5 named it explicitly; fixing only the inline call sites must fail the new test."
    ]
  }
}
