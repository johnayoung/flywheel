{
  "id": "redaction-docs",
  "goal": "Update the Audit stream section of `docs/vision.md` and the audit CLI `--help` to document the read-time redaction layer and the designed-for write-time seam.",
  "prerequisites": ["audit-cli-redaction"],
  "tags": ["redaction", "docs"],
  "context": {
    "relevant": [
      ".workflow/specs/00014-FEATURE-audit-redaction-layer.md",
      "docs/vision.md",
      "src/flywheel/audit/_cli.py"
    ],
    "constraints": [
      "Update the 'Audit stream' section of docs/vision.md (around line 46) to point at the read-time redaction layer and state that the store remains the unredacted, sensitive-by-default source of truth.",
      "Document the write-time persistence seam as a designed-for follow-up that reuses the same Redactor abstraction -- not built in this feature.",
      "The audit CLI --help includes a short usage section covering --redact/--redact-policy/--raw/--dry-run and the best-effort caveat (NFR-1).",
      "Follow the repo docs style: extremely concise, examples over prose.",
      "Commit with a Conventional Commits message before emitting intent=verify."
    ],
    "non_goals": [
      "No code/behavior changes beyond --help text already wired in audit-cli-redaction.",
      "Do not create new .md files."
    ]
  },
  "graders": [
    { "type": "command", "run": "grep -qi 'redact' docs/vision.md", "name": "vision-mentions-redaction" },
    { "type": "command", "run": "uv run python -m flywheel.audit --help | grep -q -- '--raw'", "name": "help-documents-raw" },
    { "type": "command", "run": "uv run pytest", "name": "full-suite" }
  ]
}
