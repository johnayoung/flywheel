{
  "id": "tui-live-rows-seam",
  "goal": "collect_live_rows(), LiveRunRow, and the policy-driven store-path resolution are importable from flywheel_orchestrator as public names, with no behavior change to any existing CLI surface.",
  "tags": ["orchestrator", "refactor", "tui"],
  "context": {
    "relevant": [
      ".flywheel/specs/00020-FEATURE-tui-operator-console.md",
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_workflow.py -- LiveRunRow (:707), collect_live_rows (:892), _load_effective_policy (:600), _resolve_db_path (:631) are the seams to expose",
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/__init__.py -- where the public names belong"
    ],
    "references": [
      "packages/flywheel-orchestrator/src/flywheel_orchestrator/_workflow.py:1091 -- _cmd_live shows exactly how the existing consumer resolves the store and calls collect_live_rows; the public seam must support this same call shape"
    ],
    "constraints": [
      "Pure refactor: flywheel-orchestrate live/status and every other subcommand behave identically; existing tests pass with at most import-path updates.",
      "A downstream package must be able to resolve the store path exactly the way orchestrate does (explicit --db value, else policy [paths] db, else the .flywheel/flywheel.sqlite default) without importing private _workflow names.",
      "Run mcp__ide__getDiagnostics on every .py file touched and fix all Pylance errors in the same turn.",
      "Commit with a Conventional Commits refactor: message before emitting intent=verify."
    ],
    "non_goals": [
      "No TUI code in this task.",
      "No protocol-level rewrite of collect_live_rows -- it stays SQLite-backed exactly as today.",
      "No changes to flywheel core."
    ]
  },
  "graders": [
    { "type": "command", "run": "uv run pytest", "name": "tests" },
    { "type": "command", "run": "git log -1 --format=%s | grep -qE '^(refactor|feat)'", "name": "commit-landed" }
  ]
}
