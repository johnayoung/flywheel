{
  "id": "lifecycle-awaiting-approval-state",
  "goal": "Add a non-terminal AWAITING_APPROVAL status with edges VALIDATING->AWAITING_APPROVAL, AWAITING_APPROVAL->DONE, and AWAITING_APPROVAL->FAILED_VALIDATION, plus an awaiting_manual_ordinal field on Lifecycle that clears on every ->READY/->DONE/->FAILED_VALIDATION transition.",
  "tags": ["manual-gate", "lifecycle"],
  "context": {
    "relevant": [
      ".workflow/specs/00016-FEATURE-manual-grader-approval-gate.md",
      "src/flywheel/lifecycle.py",
      "tests/test_lifecycle.py",
      "tests/test_lifecycle_module_purity.py"
    ],
    "references": [
      "src/flywheel/lifecycle.py:31 -- _VALID_EDGES, where the three new edges are added",
      "src/flywheel/lifecycle.py:104,154,187 -- blocked_requires_json field, its clearing in transition_to, and its load in _from_persisted; awaiting_manual_ordinal mirrors all three exactly"
    ],
    "constraints": [
      "AWAITING_APPROVAL must NOT be added to _REQUIRES_ERROR (lifecycle.py:57) or _RETRY_SOURCE_STATUSES (lifecycle.py:61): entering it requires no error and consumes no retry budget.",
      "Add awaiting_manual_ordinal: int | None = None to the Lifecycle dataclass; clear it to None on the ->READY, ->DONE, and ->FAILED_VALIDATION edges alongside the existing blocked_requires_json clearing in transition_to; load it in _from_persisted.",
      "flywheel.lifecycle stays pure: no json/pathlib/io/SDK imports. tests/test_lifecycle_module_purity.py must still pass unmodified.",
      "Add tests to tests/test_lifecycle.py covering each new edge is permitted, that every other out-of-AWAITING_APPROVAL transition raises LifecycleTransitionError, that AWAITING_APPROVAL consumes no retry budget and is_retry_eligible is unaffected, and that awaiting_manual_ordinal clears on the three edges.",
      "Call mcp__ide__getDiagnostics on src/flywheel/lifecycle.py and any touched test file and fix all type errors before verify.",
      "Commit with a Conventional Commits message before emitting intent=verify."
    ],
    "non_goals": [
      "No store/schema/persistence changes (persist-awaiting-manual-ordinal owns those).",
      "No harness wiring, no docs edits."
    ]
  },
  "graders": [
    { "type": "command", "run": "uv run pytest tests/test_lifecycle.py tests/test_lifecycle_module_purity.py", "name": "lifecycle-tests" }
  ]
}
