{
  "id": "task-command-emit-verify",
  "goal": "Update .claude/commands/task.md and .claude/commands/define.md so a loop-path spec or diff yields an in-loop-verification task slot (tagged in-loop-verification) wired to drive the real orchestrate loop with a scripted invoker and exercise the real prior-version migration.",
  "prerequisites": ["archive-gate-loop-path"],
  "tags": ["loop-path-gate", "commands", "docs"],
  "context": {
    "relevant": [
      ".workflow/specs/00017-FEATURE-in-loop-verification-gate.md",
      ".claude/commands/task.md",
      ".claude/commands/define.md"
    ],
    "references": [
      "Spec FR-3 (drive both loop-produced ends: harness park AND reactive-sweep apply) and FR-4 (seed v(N-1), run the real forward migration, assert via SqliteStore) define what an emitted verify slot must require.",
      "Spec Behavior Specification -> Trigger Set -- the conditions under which /task and /define must emit the slot.",
      "tests/test_worker.py::test_run_once_merges_completed_task -- the model the slot's authoring guidance should point at for driving a full real-loop cycle with a scripted invoke."
    ],
    "constraints": [
      "When the spec/diff falls in the trigger set, the command emits one in-loop-verification task slot tagged `in-loop-verification` whose command grader drives real orchestrate (scripted invoker) and, for schema features, a v(N-1)-seeded store migrated forward asserted through SqliteStore.",
      "Emit only the slot plus authoring guidance -- do NOT auto-generate the fixture/assertion content (spec Out of Scope); the human writes the test body.",
      "Reference the trigger set so authors know when the slot is required and the gate (archive-gate-loop-path) recognizes the `in-loop-verification` tag.",
      "Be concise -- engineers scan; no emojis.",
      "Commit the change with a Conventional Commits message (docs: ...) before emitting intent=verify."
    ],
    "non_goals": [
      "Do not change the archive gate or marker code.",
      "Do not run a paid agent as part of the emitted slot's grader."
    ]
  },
  "graders": [
    { "type": "command", "run": "grep -q 'in-loop-verification' .claude/commands/task.md && grep -q 'in-loop-verification' .claude/commands/define.md", "name": "slot-documented" },
    { "type": "command", "run": "git log -1 --format=%s | grep -q '^docs:'", "name": "commit-landed" }
  ]
}
