{
  "id": "recovery-handoff-prompt",
  "goal": "prompt.py renders a `# Recovery handoff` section from a handoff summary carried on IterationInputs, mirroring _section_reviewer_feedback.",
  "tags": ["context-recovery", "prompt"],
  "context": {
    "relevant": [
      "src/flywheel/prompt.py",
      "tests/test_prompt.py",
      ".workflow/specs/00018-FEATURE-context-recovery-policy.md"
    ],
    "references": [
      "src/flywheel/prompt.py:168 -- _section_reviewer_feedback is the structural precedent for the new _section_recovery_handoff section",
      "src/flywheel/prompt.py:88 -- IterationInputs is where the new optional handoff field (defaulting to None/empty) belongs, alongside prior_rubric_findings"
    ],
    "constraints": [
      "prompt.py must stay pure (no IO, no SDK imports, no template-file reads) -- enforced by tests/test_prompt_module_purity.py.",
      "The new IterationInputs field MUST default (e.g. None) so every existing build_iteration_prompt caller is unchanged and the section renders only when a handoff is present.",
      "Output stays byte-identical for byte-identical inputs -- no timestamps, no IO.",
      "Render the structured handoff (work done, work remaining, key decisions, suggested next step) per spec FR-3; place the section in a fixed order like the reviewer-feedback section.",
      "Commit with a Conventional Commits message (feat: ...) before emitting intent=verify."
    ],
    "non_goals": [
      "Do not have the harness populate the field here -- this task only adds the field and the renderer; the action task wires the harness to supply it.",
      "Do not change the reviewer-feedback section's output."
    ],
    "edge_cases": [
      "Absent handoff (default) -> no `# Recovery handoff` section, identical to today's prompt.",
      "Handoff present alongside reviewer feedback -> both sections render in a deterministic fixed order."
    ]
  },
  "graders": [
    { "type": "command", "run": "uv run pytest tests/test_prompt.py tests/test_prompt_module_purity.py", "name": "tests" }
  ]
}
