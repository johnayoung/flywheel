{
  "id": "orchestrator-reactive-resolve",
  "goal": "Have the orchestrator's reactive sweep call resolve_manual_approval on AWAITING_APPROVAL lifecycles alongside recheck_blocked_lifecycle, and exempt AWAITING_APPROVAL from finalize_stranded_lifecycle so a parked gate survives worker restart untouched.",
  "prerequisites": ["resolve-manual-approval"],
  "tags": ["manual-gate", "orchestrator", "durability"],
  "context": {
    "relevant": [
      ".workflow/specs/00016-FEATURE-manual-grader-approval-gate.md",
      "src/flywheel/orchestrator.py",
      "src/flywheel/harness.py",
      "tests/test_orchestrator.py",
      "tests/test_harness.py"
    ],
    "references": [
      "src/flywheel/orchestrator.py:169-177 -- _is_blocked_interrupted, the selection predicate keyed on row.latest_status == Status.INTERRUPTED; add a sibling _is_awaiting_approval keyed on row.latest_status == Status.AWAITING_APPROVAL (the row carries latest_status directly, independent of task_state classification)",
      "src/flywheel/orchestrator.py:343-406 -- the reactive run-loop pass that calls recheck_blocked_lifecycle (~:406); call resolve_manual_approval on awaiting-approval rows in the same pass",
      "src/flywheel/harness.py:2765 -- finalize_stranded_lifecycle; add AWAITING_APPROVAL to the exempted (not-stranded) statuses",
      "src/flywheel/workflow.py:361 -- _STRANDED_STATUSES already excludes AWAITING_APPROVAL (it is {RUNNING, VALIDATING}); confirm no change is needed there"
    ],
    "constraints": [
      "The reactive sweep applies a pending approve/reject on the next tick and advances the lifecycle; with no pending command the lifecycle remains AWAITING_APPROVAL.",
      "finalize_stranded_lifecycle must leave an AWAITING_APPROVAL lifecycle and its already-finalized attempt untouched (the attempt was finalized SUCCEEDED at gate entry, so the open-attempt strand rule is unaffected -- only the parked status needs exempting).",
      "Add tests/test_orchestrator.py cases: a pending approve is applied on the next reactive tick and the lifecycle advances; with no pending command the lifecycle stays AWAITING_APPROVAL. Add a tests/test_harness.py (or stranded-lifecycle) case that finalize_stranded_lifecycle leaves an AWAITING_APPROVAL lifecycle untouched.",
      "Call mcp__ide__getDiagnostics on every touched .py file and fix all type errors before verify.",
      "Commit with a Conventional Commits message before emitting intent=verify."
    ],
    "non_goals": [
      "No new resolver logic (resolve_manual_approval is reused as-is).",
      "No timeout/auto-resolution machinery."
    ]
  },
  "graders": [
    { "type": "command", "run": "uv run pytest tests/test_orchestrator.py tests/test_harness.py", "name": "orchestrator-durability-tests" },
    { "type": "command", "run": "uv run pytest", "name": "full-suite" }
  ]
}
