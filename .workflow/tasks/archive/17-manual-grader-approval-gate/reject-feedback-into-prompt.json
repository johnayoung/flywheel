{
  "id": "reject-feedback-into-prompt",
  "goal": "Generalize the prior-attempt feedback collector to also gather failing manual grader receipts and render them in the iteration prompt's reviewer-feedback section labeled distinctly from rubric findings (e.g. \"manual <name> (operator): <feedback>\").",
  "prerequisites": ["resolve-manual-approval"],
  "tags": ["manual-gate", "prompt", "retry-feedback"],
  "context": {
    "relevant": [
      ".workflow/specs/00016-FEATURE-manual-grader-approval-gate.md",
      "src/flywheel/harness.py",
      "src/flywheel/prompt.py",
      "tests/test_prompt.py",
      "tests/test_prompt_module_purity.py",
      "tests/test_harness.py"
    ],
    "references": [
      "src/flywheel/harness.py:928-975 -- _collect_prior_rubric_findings; generalize it (or pair a sibling collector) to also gather the prior attempt's failing manual receipts",
      "src/flywheel/prompt.py -- the spec-00005 '# Reviewer feedback' section; render manual rejections with the operator label"
    ],
    "constraints": [
      "flywheel.prompt stays pure: it receives plain finding data, never a store reference; tests/test_prompt_module_purity.py must still pass.",
      "Mixed rubric + manual findings render deterministically ordered by (attempt_number, ordinal); empty findings render no section (byte-identical to today).",
      "Add tests/test_prompt.py cases: a manual rejection renders in the reviewer-feedback section with the operator label; empty findings render no section; mixed rubric+manual findings render in (attempt_number, ordinal) order. Add a tests/test_harness.py end-to-end case: a rejected first attempt followed by a second attempt whose prompt contains the operator feedback.",
      "Call mcp__ide__getDiagnostics on every touched .py file and fix all type errors before verify.",
      "Commit with a Conventional Commits message before emitting intent=verify."
    ],
    "non_goals": [
      "No change to how rubric findings are rendered beyond adding the manual label.",
      "No new control verbs or resolver logic."
    ]
  },
  "graders": [
    { "type": "command", "run": "uv run pytest tests/test_prompt.py tests/test_prompt_module_purity.py tests/test_harness.py", "name": "prompt-feedback-tests" }
  ]
}
