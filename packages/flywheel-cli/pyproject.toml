[project]
name = "flywheel-cli"
version = "0.1.0"
description = "Unified `fw` command router and operator console: dashboard + session screen + persistent input bar; delegates verbs to the existing single-task, orchestrator, worker, and audit entry points."
requires-python = ">=3.13"
authors = [
    { name = "John Young", email = "john.anto.young@gmail.com" }
]
dependencies = [
    "flywheel",
    "flywheel-orchestrator",
    "flywheel-worktree",
    "textual>=0.85",
]

[project.scripts]
fw = "flywheel_cli._cli:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
