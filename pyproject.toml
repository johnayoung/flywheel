[project]
name = "flywheel"
version = "0.1.0"
description = "Production-grade orchestration loop for AI coding agents."
readme = "README.md"
authors = [
    { name = "John Young", email = "john.anto.young@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
